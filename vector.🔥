# defines vector struct

struct Vector:

    var contents: ListLiteral

    fn __init__(inout self, *args):
        pass

    fn __copyinit__(inout self, existing: Self):
        for i in range(len(self.contents)):
            self.contents[i] = existing.contents[i]
