from Neural.model import Sequential
from Neural.activations import softmax, relu, sigmoid
from Neural.layers import Dense

fn main() raises:
  var model = Sequential(
    (
      Dense(784, relu), 
      Dense(16, relu),
      Dense(16, relu),
      Dense(10, relu)
    )
  )
